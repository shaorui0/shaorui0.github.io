# shaorui0.github.io

Personal site — https://shaorui0.github.io/

- `index.html` — landing page (bilingual, `#/en` `#/cn`)
- `deep.html` — the deeper résumé: nine-axis capability radar, domain pages, on-call track record
- `latex/` — the printable LaTeX résumés

Long-form articles are not in this repo. They live on the blog
(https://shaorui0.github.io/tech), reachable from the links on the landing page.
`shaorui0/resume` now just redirects here.

Generated from `adhoc_jobs/dynamic_resume_site/` in my private workspace:
`content/*.md` is the single source, `build_content.py` injects the article index,
`export_to_blog.py` publishes the articles to the blog.
